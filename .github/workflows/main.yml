name: OMR to MIDI Conversion

on:
  # Triggers the workflow on pushes to the 'main' branch
  push:
    branches: [ "main" ]
    paths:
      - 'scores/*.{png,jpg,jpeg,pdf}'
  # Allows you to manually run the workflow
  workflow_dispatch:

jobs:
  convert_score:
    runs-on: ubuntu-latest
    
    env:
      INPUT_DIR: scores
      OUTPUT_DIR: music_data

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4
        
      - name: ⚙️ Set Up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 📦 Install Python Dependencies
        run: pip install -r requirements.txt

      - name: 🧹 Create Output Directory and Input List
        run: |
          mkdir -p ${{ env.OUTPUT_DIR }}
          # Create a file list using the correct path /input/score.png format
          find ${{ env.INPUT_DIR }} -type f -regex ".*\.\(png\|jpg\|jpeg\|pdf\)" -printf '/input/%f\n' > input_files.txt
          
          # Exit if no input files are found
          if ! [ -s input_files.txt ]; then
            echo "::warning::No score files found in 'scores/'. Skipping OMR step."
            exit 0
          fi
          echo "Found scores to process."

      - name: 🎵 Run Audiveris (OMR to MusicXML) via Docker CLI 
        # FIX: Re-inserting the explicit '-export' flag to force MusicXML generation.
        run: |
          echo "Starting OMR process using Audiveris Docker (toprock, high memory, forcing export)..."
          docker run --rm \
            -v ${{ github.workspace }}/${{ env.INPUT_DIR }}:/input \
            -v ${{ github.workspace }}/${{ env.OUTPUT_DIR }}:/output \
            -v ${{ github.workspace }}/input_files.txt:/tmp/input_files.txt \
            -e JAVA_TOOL_OPTIONS='-Xmx4g' \
            toprock/audiveris:latest \
            /audiveris-extract/bin/Audiveris \
            -batch \
            -output /output \
            -export \
            @/tmp/input_files.txt
          echo "Audiveris OMR complete."

      - name: 🔎 Verify Audiveris Output
        # Uses recursive check to find files inside subdirectories
        run: |
          echo "Listing contents of ${{ env.OUTPUT_DIR }} after Audiveris run (recursive check):"
          ls -R ${{ env.OUTPUT_DIR }}
          
          if [ -z "$(find ${{ env.OUTPUT_DIR }} -name "*.mxl" -print -quit 2>/dev/null)" ]; then
            echo "::error::Audiveris failed to generate any MusicXML (.mxl) files. Please check the log files inside the subdirectories for Java errors."
            exit 1 # Fail the job here if no MXL is found
          else
            echo "✅ MusicXML files found! Proceeding to MIDI conversion."
          fi

      - name: 🎼 Run MusicXML to MIDI Conversion (Python)
        # Uses 'find' to recursively locate all .mxl files for processing
        run: |
          echo "Starting MusicXML to MIDI conversion..."
          find ${{ env.OUTPUT_DIR }} -name "*.mxl" | while read XML_FILE; do
            if [ -f "$XML_FILE" ]; then
              echo "Converting $XML_FILE to MIDI..."
              python scripts/xml_to_midi.py "$XML_FILE"
            fi
          done
          echo "Conversion complete."

      - name: ⬆️ Upload Generated MIDI/XML Files (as Artifact)
        # Uploads all contents of the output directory, including sub-folders
        uses: actions/upload-artifact@v4
        with:
          name: converted-music-files
          path: ${{ env.OUTPUT_DIR }}
          retention-days: 7

      - name: 🔄 Commit Generated Files
        # Uses **/*.{mxl,mid} to find files recursively and uses --allow-empty
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'ci: generate MusicXML and MIDI from new scores'
          file_pattern: ${{ env.OUTPUT_DIR }}/**/*.{mxl,mid}
          skip_dirty_check: true
          commit_options: '--allow-empty'
